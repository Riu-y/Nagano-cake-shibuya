<h1>配送先登録/一覧</h1>




  <%= form_for @shipping_address, url:  customers_customer_shipping_addresses_path(@customer) do |f| %>

  <div class="field">
    <%= f.label :郵便番号（ハイフンなし） %>
    <%= f.text_field :shipping_postal_code, autofocus: true, autocomplete:"", placeholder:"郵便番号" %>
  </div>
  <div class="field">
    <%= f.label :住所 %>
    <%= f.text_field :shipping_address, autofocus: true, autocomplete:"", placeholder:"住所" %>
  </div>

  <div class="field">
    <%= f.label :宛名 %>
    <%= f.text_field :shipping_name, autofocus: true, autocomplete:"", placeholder:"宛名" %>
  </div>

   <div class="actions">
    <%= f.submit "登録する" %>
  </div>
  <% end %>

<table class="table">
  <thead>
<tr>
  <td>郵便番号     </td>
  <td>住所     </td>
  <td>宛名</td>
</tr>
</thead>

<tbody>
    <% @shipping_addresses.each do |shipping| %>
    <tr>
      
      <td><%= shipping.shipping_postal_code%></td>
      <td><%=shipping.shipping_address%></td>
      <td><%=shipping.shipping_name%></td>
      <td><%= link_to "Destroy",@shipping_address, method: :delete, data: {confirm: "本当に削除してもよろしいですか？"}, class: "btn-sm btn-danger destroy_book_#{@shipping_address.id}" %></td>
    </tr>
    <% end %>
  </tbody>
</table>







